REVERSE RUSTY — BENCHMARK RUNBOOK & CAPTURE LOG
============================================================================================

PURPOSE
  The reference for running Reverse Rusty's benchmarks and judging the result. It answers three
  questions for whoever runs them next:
    1. WHAT to run (commands, args, runtime budget) ................ "How to run"
    2. WHAT must hold regardless of hardware (the regression gate) . "Invariants"
    3. WHAT the numbers were last time, and on what machine ........ "Capture log"

  Read INVARIANTS first. Those numbers are fixed by the data + the algorithm, NOT the CPU, so
  they must reproduce on ANY machine. If one drifts, the engine's *behaviour* changed —
  investigate before trusting anything else. THROUGHPUT, by contrast, is hardware- and
  load-dependent: only compare it against a prior run ON THE SAME MACHINE, never across
  machines, and never treat a cross-machine delta as a regression.

  Numbers here are distilled. Full console output of a run is ephemeral (e.g. /tmp at capture
  time); record the distilled result in the capture log below — append, don't overwrite. This
  file is the durable, version-controlled record. CI (GitHub Actions) also runs the benchmarks on
  every PR and uploads the raw console output as an artifact, but does NOT gate on them: throughput
  is machine-dependent, so the invariants below stay a manual comparison (see DECISIONS ADR-024).

--------------------------------------------------------------------------------------------
HOW TO RUN
--------------------------------------------------------------------------------------------
  cd engine
  export CARGO_TARGET_DIR=/tmp/reverse-rusty-target            # build off-tree (see CLAUDE.md)

  # ---- correctness (the contract; fast) ----
  cargo test --release --test oracle                  # differential oracle: false_neg/pos = 0
  cargo test --release --test stress                  # 15 pressure tests (churn, par==seq, ghosts)
  cargo test --release --test stress \
      ten_million_queries_mixed_ops -- --ignored --nocapture   # 10M-scale soak (needs ~4+ GiB)
  cargo test --release --test cluster_soak \
      -- --ignored --nocapture                        # 20M multi-shard scale soak (ADR-104; ~4 min,
                                                      #   ~16 GiB RAM + ~3.2 GiB temp disk; scale down
                                                      #   with RR_CLUSTER_SOAK_QUERIES/_TITLES/_SHARDS)

  # ---- throughput / structure ----
  cargo run --release --bin bench -- 1000000 20000 0.0      # PURE SELECTIVE (clean cand count)
  cargo run --release --bin bench -- 1000000 20000 0.05     # +broad lane: inline cost AND the
                                                            #   columnar batch win + amortization sweep
  cargo run --release --bin segbench -- 300000 3000 0.0     # read-amp vs 1/2/4/8 segments
  cargo run --release --bin snapbench -- 1000000 200        # snapshot-publish cost (ADR-016)
  cargo run --release --bin clusterbench -- 100000 5000 8 0.05  # CLUSTER fan-out + candidate structure
  cargo run --release --bin rankbench -- 20000 500 8 275775489  # local/distributed bounded top-K + fetch
  cargo run --release --bin learn -- 500000 50 0.30         # NPMI corpus learner (see appendix)

  bench args:        <num_queries> <num_titles> <broad_frac> <skew> <seed> [reps]
  segbench args:     <num_queries> <num_titles> <broad_frac> <seed>
  snapbench args:    <num_queries> <iters>
  clusterbench args: <num_queries> <num_titles> <num_shards> <broad_frac> <seed>
  rankbench args:    <num_queries> <num_titles> <num_shards> <seed>
  Seeds default to 0x00C0FFEE so runs are reproducible. Budget: each is <~30s except the 5M+
  bench memory passes (they build the corpus to disk twice — a few minutes) and the 10M soak.

--------------------------------------------------------------------------------------------
INVARIANTS — the regression gate (machine-INDEPENDENT; must reproduce)
--------------------------------------------------------------------------------------------
  Last verified 2026-05-30 (Apple M4 Max, 16-core) AND 2026-05-27 (aarch64, 4-core): identical.

  CANDIDATES / TITLE  (the core selectivity metric — the whole engine exists to keep this small
  and FLAT as the corpus grows):
    segbench 300000 3000 0.0    K=1 -> 53.48   K=2 -> 55.11   K=4 -> 56.67   K=8 -> 57.68
    bench 1000000 20000 0.0     avg 54.5  (p95 96, p99 112)   [postings/title == cands/title]
    -> ~54, flat regardless of corpus size; postings == cands on the selective (no-broad) path.

  FILTER SKIP %  (per-segment anchor filter; rises with segment count):
    segbench K=1/2/4/8   ->   ~46% / ~63% / ~76% / ~86%      (filter heap 0.02–0.07 MB)

  COST-CLASS SPLIT  (bench 1000000 20000 0.05):   ~99.5% A | ~0.2% B | ~0.2% C | 0 D
    (broad_frac=0.0 -> 100% A.  class D must always be 0 for generated data.)

  BROAD-LANE AMORTIZATION  (the columnar batch evaluator; ADR-026) — bench 1000000 20000 0.05:
    The broad lane is evaluated once per title-batch instead of once per title. broad_postings_scanned
    per pass (the broad subset of postings; the selective lane is batch-size-INVARIANT) falls ~1/bs as
    the batch grows — each hot broad posting is scanned once per batch, not once per title. The ratio
    broad_post(bs=1) / broad_post(bs=N) is the amortization factor; it reproduces on any box.
      bs                  1         8        64       256      1024
      broad_post/pass  5124096  3453872   696112   175696    44480
      amortization x      1x      1.5x       7x       29x      115x     (= broad_post(bs=1)/broad_post(bs))
      broad_batches    20000     2500       313       79        20      (== ceil(num_titles / bs))
    All generated broad queries are pure-anchor, so broad_qs_eval == 0 (100% take the skip-verify fast
    path; the bitmap-verify path is exercised by tests/broad_batch.rs, not this corpus). These numbers
    are fixed by the data + algorithm (seed 0x00C0FFEE), NOT the CPU — they must reproduce.
    CORRECTNESS: match_titles_batch[t] is byte-identical to per-title match_title(t, broad=true) for
    ALL t and ALL bs (tests/broad_batch.rs, 7/7) — batch size is a perf knob, never a semantic one.

  CLUSTER FAN-OUT  (clusterbench 100000 5000 8 0.05) — fixed by the corpus + the ring, NOT the CPU:
    shards probed / title @K=8:   avg 3.84   p50 4   p95 5   p99 6   max 7     (of 8 shards)
    FAN-OUT SCALING (same corpus, K = 1 / 2 / 4 / 8 / 16 / 32):
      avg_fanout   1.00 / 1.90 / 3.08 / 3.84 / 4.36 / 4.60     <- BOUNDED: saturates ~4-5, never -> N
      p99_fanout      1 /    2 /    4 /    6 /    7 /    7
    candidates/title (85.64) AND broad share (26.9%) are IDENTICAL at every K — the cluster distributes
    selectivity without inflating it: the probed shards' candidate union is exactly the single-node
    candidate set, however many shards K splits it across (a corollary of the lossless cover, ADR-027).
    -> the load-bearing cluster invariants: content routing touches a handful of shards (~2-5, never
       all N), and candidates/title does NOT grow with K. Both reproduce on any machine.

  CLUSTER SCALE SOAK  (cluster_soak, ADR-104 — 20M queries, K=8, durable, in-process; seed
  0x2000_0000, clusterbench-convention pools Q/40 / Q/100.  The scale half of ADR-065 criterion
  12; a ONE-OFF acceptance run — in no gate/CI — kept reproducible in-tree):
    correctness (must be exactly these):  differential mismatches 0 (pre- AND post-mutation,
      50k titles vs the single-node reference);  sentinel misses 0 (pre / post-mutation / after
      reopen);  ghosts 0;  reopen: 2000-title subset + sentinels byte-identical;  live-add
      retrievability failures 0 (1k sampled synthetic-ID adds)
    structure @20M (fixed by data + ring, must reproduce):
      fan-out @K=8:  avg 3.18  p50 3  p95 5  p99 5  max 7   (of 8)   <- bounded at 200x the 100k pin
      per-shard entries: min 2,399,795 / max 2,847,093  (max/min 1.19, balanced)
      stored entries 20,613,457 over 20,002,000 queries (broad replicates to all K, ADR-080);
        classes A/B/C/D = 19,914,649 / 350,192 / 348,616 / 0
      candidates/title: unique 10,035.55 (selective 5,164.52 + broad 4,871.04, broad share 48.5%)
        -> GROWS with Q by design with the broad lane ON (lineage 85.64 @100k -> 682 @1M ->
           10,036 @20M; what the ADR-026 columnar lane amortizes). NOT a flatness violation:
           the flat-~54 claim is the broad-OFF selective path — re-verified at 20M below.

  SELECTIVE FLATNESS AT 20M  (bench 20000000 20000 0.0 — fixed default pools; extends the 1M/5M
  pin 4x past the prior largest run):
    avg unique cand/title 54.56  (p95 96, p99 112, max 152)   <- the flat-~54 invariant HOLDS at 20M
    main max posting len 104, #postings>1024: 0;  classes 20M A / 0 B / 0 C / 0 D
    dict 683,877 features (84.6 MB resident, 4.2 B/q — bounded);  resident total 5.2 B/q
    (retain_source=false) / 109.0 B/q (retain_source=true)
    cross-check (bench 20000000 20000 0.05, same fixed pools — SINGLE-node): unique 6,616.65
    (broad 4,977.71, p99 43,741) -> the broad-on growth reproduces without any cluster code:
    it is a property of a corpus containing broad-intent queries, not a cluster artifact;
    columnar batch = 4.3x over inline broad at this scale (the ADR-026 mitigation, bs=256)

  CORRECTNESS  (must be exactly these — this is the contract):
    oracle:   false_neg = 0,  false_pos = 0      (~109k matches over 40k queries x 4k titles)
    stress:   15/15 pass;  ghosts (deleted ids in results) = 0;  par == seq, 0 mismatches
    10M soak: 0 ghosts, par==seq 0 mismatches, compaction reclaims tombstones, sources consistent

  RESIDENT MEMORY  (persistent/mmap profile — machine-independent; ADR-020), bench @1M:
    retain_source=false -> ~5–6 B/query     (sources off-heap — the production profile)
    retain_source=true  -> ~115–120 B/query (the source store, ~113 B/q, dominates)
    components: dict ~5 B/q (bounded by vocab), alive overlay ~1 B/q, logical_index ~0 (mmap'd)

--------------------------------------------------------------------------------------------
MACHINE-DEPENDENT — record the machine; compare only same-machine over time
--------------------------------------------------------------------------------------------
  Throughput (titles/sec/core selective & broad, build q/s, updates/s, snapshot publish/s,
  parallel speedup) scales with the CPU, memory bandwidth, and current machine load. Do NOT
  compare across machines or call a delta a regression without re-running the prior machine.
  Parallel speedup saturates on memory bandwidth (and on big.LITTLE CPUs the efficiency cores
  drag the rayon reduce), so expect sub-linear scaling past ~4 threads — the 4-core reference's
  "~3.8x on 4 threads" does NOT extrapolate to a 16-core box.

  macOS caveat: bench's "process RSS" reads /proc/self/status (Linux-only) and prints 0 on
  macOS. Ignore it; use the persistent/mmap RESIDENT breakdown above, which is computed from
  Engine::metrics() and is machine-independent.

--------------------------------------------------------------------------------------------
CAPTURE LOG  (newest first — append a block per run; lead with the machine)
--------------------------------------------------------------------------------------------
[2026-07-16] Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64. ADR-110 DISTRIBUTED
              TOP-K + QUERY-THEN-FETCH. rankbench 20000 500 8 275775489, release.
              Full table: ranked-percolation-baseline.txt. Timings are indicative;
              checksums/row bounds/byte totals are seed-fixed.
  INVARIANTS:
    semantic checksums UNCHANGED from Increment 0:
      ordinary            local=b1afc6fc70a0a6e9 cluster=9190396adafa380f
      broad-heavy         local=bb248a758615fda0 cluster=0b64b11f5259ff1c
      body-duplicate      local=0e39cfc896f99281 cluster=4d3ea2d4e20345b0
      placement-duplicate local=0ec5dda03607d01f cluster=77575a4edc1f29e7
    every title/K asserted distributed winners + totals == local collect-all/full-sort/truncate;
      rows_received <= K × routed_shards (all workloads/K); ADR-109 compatibility emissions now
      have duplicates=0 while the compatibility checksum stays byte-identical
    actual bounded shard rows for K=10/100/1000/10000:
      ordinary 500/500/500/500; broad-heavy 878/7178/18313/18313;
      body-duplicate 792/7160/7512/7512; placement-duplicate 500/500/500/500
    winner fetch bytes for K=10/100/1000/10000:
      ordinary 25908 each; broad-heavy 6185/44099/111177/111177;
      body-duplicate 5304/43536/45656/45656; placement-duplicate 15780 each
    shard_result_bytes=0 is expected here: rankbench uses the in-process cluster seam; exact remote
      protobuf bytes are measured and cap-checked by the real-gRPC oracle
  TIMINGS (indicative): distributed collect+merge across all titles ranged 23.0–52.3 ms by
    workload/K; winner fetch ranged 0.30–1.89 ms. Local bounded match+rank ranged 1.15–3.14 ms.

[2026-07-03] Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64.  20M COMBINED RECOVERY
              (hot tier ADR-105 + dedup Stage A ADR-106 — Broad-Query Cost Program
              increments 1+2, measured together per the corrected reading: the corpus's
              32x is an identical-query concentration, so the recovery is dedup-driven
              and the hot tier is pinned separately).  bench 20000000 20000 0.05 2.0
              12648430 25 [theta] [dedup];  soak RR_CLUSTER_SOAK_THETA=[theta].
              Interactive box — throughput indicative; structural numbers seed-fixed.
  INVARIANTS (machine-independent; baseline = the 2026-07-03 20M BROAD-BEARING BASELINE):
    classes: dedup NEVER reclassifies (A/B/C/D byte-identical to baseline at theta=0);
      theta=1024 moves EXACTLY would_be_hot: A 19,912,766 -> 19,911,984 (-782), H = 782,
      and the theta-on run's would_be_hot reads 0 (nothing left to move) — the
      observe-counter == enforcement pin, seed-fixed
    dedup telemetry: 285,945 joined / 20,000,000 bodies; distinct est 19,697,575
      (~300k global duplicates; the ~43.5k-per-shape identical broad-intent population
      + generated family repeats); broad=0.0 corpus: 195,998 joined / est 19,811,494
    main max posting 43,533 -> 103  (#main postings >1024: 1 -> 0) — the fat shared
      class-B pair posting collapsed to its group leaders
    cand/title unique 6,616.65 -> 53.75 (p95 43,701 -> 95;  p99 111;  max 136) — the
      broad-FREE flat-~54 pin now holds on the BROAD-BEARING corpus
    avg matches/title 6,562.671 (UNCHANGED — emission is the answer, not waste; the
      recovery moves scan+verify volume, never the result set)
    broad lane: broad_post/pass @bs=1 99,554,245 -> 2,281 (the identical class-C shapes
      grouped too); broad cand/title 4,977.71 -> 0.11;  bs sweep 1x/1x/7x/29x/114x
      amortization shape preserved;  prefilt_skip 0 (unchanged, corpus-shaped)
    HOT-EMPTY PIN (broad=0.0, theta 0 vs 1024, dedup on both): H=0 both; candidates
      byte-identical (54.03, p95 95, p99 111, max 151); 423,045 vs 416,172 t/s/core
      per-title selective (~1.6%, run noise) — the hot tier is FREE when empty
  THROUGHPUT — indicative (all dedup-on; baseline in parens):
    per-title selective, broad=0.05:  84,708 t/s/core  (13,383 -> 6.3x recovered;
      residual vs the broad-free 423k lane is EMISSION-bound: ~6.5k true matches/title
      x ~550M emitted ids/s — levers 1/4 territory, not scan waste)
    batch-path selective (broad OFF): 391,388 t/s  (93,452 -> 4.2x)
    columnar broad bs=256:            192,343 t/s  (69,463 -> 2.8x);  inline broad
      34,558 (16,377 -> 2.1x)
    theta=1024 combined run within noise of dedup-only (84,501 / 394,374 / 191,874) —
      at H=782 the hot tier costs nothing measurable, exactly as the corrected
      reading predicted for THIS corpus
    build 41.9s (477k q/s; baseline 44.5s — the 285,945 skipped posting inserts);
      broad=0.0 builds 44.4s (449-452k q/s both theta)
    (parallel per-title micro-bench reads 51-65k total @16T on this box — BELOW
      single-thread; the per-thread work at these throughputs is too small for the
      rayon chunking + per-thread scratch to amortize, and emission saturates write
      bandwidth. The batch path is the parallel proxy that matters: 391-712k t/s.)
  SOAK (20M + 2k sentinels, 50k titles, K=8, durable; RR_CLUSTER_SOAK_THETA):
    theta=0, dedup-on: ALL GREEN, 264.3s total, build 60.7s (3,215 MB) — classes AND
      cand/title (10,035.55; selective 5,164.52 + broad 4,871.04) IDENTICAL to the
      ADR-104 canonical run: the durable cluster FLUSHES to mmap segments, whose
      postings are always EXPANDED — Stage A's candidate-volume win is confined to
      in-memory segments by design; the durable-path counterpart is Stage B (the
      persisted indirection, roadmap increment 3, sized by the sketch above).
      0 mismatches / 0 sentinel misses / 0 ghosts / reopen identical.
    theta=1024, dedup-on: ALL GREEN, 263.7s — classes A/B/C/D/H = 19,913,851 /
      350,192 / 348,616 / 0 / 798 (A -798 = exactly the theta-hot population under
      the cluster dict; B/C byte-identical); every differential/sentinel/ghost/reopen
      check identical to theta=0 — theta is visibility-invariant at 20M cluster scale.

[2026-07-03] Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64.  20M BROAD-BEARING BASELINE
              (bench 20000000 20000 0.05, default skew 2.0 / seed 0x00C0FFEE) — the
              Broad-Query Cost Program increment-1 "BEFORE" leg (observe-first telemetry +
              lever-5a count-gate built; hot tier NOT yet built). Interactive box —
              throughput indicative; the structural numbers are seed-fixed and reproduce.
  INVARIANTS (machine-independent):
    classes A/B/C/D = 19,912,766 / 43,589 / 43,645 / 0;   would-be hot 782  <- NEW meter
    main max posting 43,533;  #main postings >1024: exactly 1;  dict 684,509 features
    cand/title unique 6,616.65 (broad 4,977.71 / selective 1,638.94; p95 43,701 p99 43,741)
    broad sweep amortization (broad_post/pass): 99,554,245 @bs=1 -> 3,447,955 @256 (29x)
      -> 872,900 @1024 (114x);  broad_qs_eval 0 (all generated broad = pure-anchor);
      prefilt_skip 0 at every bs (expected: the count-gate's shape — multi-positive broad
      candidates with batch-absent features — does not occur in this corpus; the gate is
      pinned by tests/broad_batch.rs instead)
  THROUGHPUT — indicative:  build 44.5s (450k q/s);  per-title selective 13,383 titles/s/core
    (the "32x lane": the broad-FREE corpus pins 437,730 — see the 2026-07-02 block);  parallel
    selective 26,878 t/s;  inline broad 16,377;  batch-path selective (broad OFF) 93,452;
    columnar bs=256 69,463 (4.2x inline).  Resident 5.2 B/q (no sources) / 108.7 B/q (with).
  THE CORRECTED READING (what the observe-first telemetry falsified — the reason it ships
  before enforcement):
    * The generated broad-intent population is ~130k queries (~43.5k per shape), NOT ~1M:
      gen.rs draws the 5% broad branch per FAMILY ITERATION (family_size=8), not per query.
      The prior "~956k broad-intent classified A" reading (broad-cost-program.md §3.1) was
      an arithmetic artifact of assuming per-query draws. Cross-check: the 100k clusterbench
      pin (B=223, C=212 ~= 218/shape) and the ADR-104 soak (stored B=350,192 = ~43.8k x8
      replicas, ADR-080) both fit the per-iteration reading exactly.
    * The fat 43,533-entry MAIN posting is the SHARED CLASS-B ARITY-2 PAIR posting of the
      ~43.5k identical "psa 10" broad-shape queries (B=43,589 ~= that one shape), NOT a
      rank-#65 class-A anchor. The per-title selective collapse (13.4k t/s/core; p95
      cand/title 43,701) is titles bearing BOTH pair tokens scanning that one posting.
      Consequences: (a) this corpus's realtime defect is an IDENTICAL-QUERY CONCENTRATION —
      the dedup lever's case (spec §5.1) and the ADR-080 deferred B-arity-2 cost note — and
      theta-reclassification (lever 2) does NOT move class B, so the hot tier alone will not
      recover this corpus's 32x; (b) the genuine lever-2 population here (non-top64 anchors
      past theta=1024) is would_be_hot = 782 (Zipf-head players), i.e. the top-64-cliff
      defect remains real by construction but is nearly absent from THIS synthetic corpus.
      Increment-1 acceptance needs recalibration against these facts (a fat-anchor corpus
      variant, or acceptance via the oracle stack + hot-empty pins + a constructed bench).

[2026-07-02] Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64.  20M MULTI-SHARD SCALE SOAK
              (cluster_soak, ADR-104 — the ADR-065 criterion-12 scale half) + the 20M bench
              flatness cross-check.  Interactive box — wall-clock/throughput indicative; the
              structural + correctness numbers are NOT (seed-deterministic, reproduce anywhere).
  INVARIANTS — cluster_soak 20M + 2k sentinels, 50k titles, K=8, durable (see CLUSTER SCALE SOAK
  block above; all correctness EXACT):
    differential 0 mismatches pre- AND post-mutation (319,595,408 reference matches / 50k titles)
    sentinels 0/2000 missed (pre / post-mutation / after reopen);  ghosts 0
    live-add retrievability 0/1000 failed (frozen-dict synthetic-ID adds, ADR-046)
    reopen byte-identical (2000-title subset + all sentinels);  reopened stored 20,507,108
    fan-out avg 3.18  p50 3  p95 5  p99 5  max 7  (of 8);  per-shard min 2,399,795 / max 2,847,093
    classes A/B/C/D 19,914,649 / 350,192 / 348,616 / 0;  cand/title 10,035.55 (broad 48.5%)
  TIMINGS / FOOTPRINT — indicative: gen 5.7s;  durable K=8 build 61.5s (~325k q/s placed);
    fan-out+stats pass 6.0s (~8.3k titles/s through the cluster, per-title fan-out parallelised);
    reference build 43.9s;  differentials 12.3s / 18.8s;  mutations 50.0s (320k ops incl. the
    8-shard delete fan-out);  flush+checkpoint 25.5s;  reopen 4.5s (mmap attach).
    TOTAL 233.5s;  peak RSS 16.07 GB;  data_dir 3,215 MB.
  BENCH 20M (fixed default pools) — the flatness cross-check:
    broad=0.0:  unique 54.56/title (p95 96, p99 112, max 152; max posting 104) — the flat-~54 pin
      EXTENDS 1M/5M -> 20M;  437,730 titles/s/core selective;  build 42.7s (468k q/s);
      dict 683,877 features (4.2 B/q);  resident 5.2 B/q (no sources) / 109.0 B/q (with)
    broad=0.05: unique 6,616.65 (broad 4,977.71) — single-node REPRODUCES the broad-on growth
      (a corpus property, not a cluster artifact);  columnar batch 4.3x over inline broad (bs=256)

[2026-06-02] Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64.  Cluster fan-out harness (clusterbench,
              Tier-0 observability).  Loaded interactive box — throughput indicative; the fan-out +
              candidate structural numbers are NOT (they are K-invariant and reproduce bit-for-bit).
  INVARIANTS — clusterbench 100000 5000 8 0.05 (see CLUSTER FAN-OUT block above):
    fan-out @K=8:  avg 3.84  p50 4  p95 5  p99 6  max 7   (of 8 shards)
    scaling avg_fanout  1.00/1.90/3.08/3.84/4.36/4.60  for K 1/2/4/8/16/32  (saturates ~4-5, never ->N)
    cand/title 85.64 and broad share 26.9% IDENTICAL at every K  (lossless cover: union == single-node set)
    build  A=99565 B=223 C=212;  per-shard counts min 11741 / max 13858 (shard 0 = 12728)
  THROUGHPUT — indicative, K=8, single driver thread (per-title fan-out parallelised internally):
    selective ~21.6k titles/s   |   with broad ~21.7k titles/s

[2026-05-30b] Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64.  Broad-lane columnar batch
              evaluation (ADR-026, Stage 2).  Loaded interactive box — throughput is indicative;
              the broad-amortization structural numbers are NOT (they reproduced bit-for-bit).
  INVARIANTS — broad amortization reproduced, bench 1M 20k 0.05 (see INVARIANTS block above):
    broad_post/pass  5124096 / 3453872 / 696112 / 175696 / 44480   for bs 1 / 8 / 64 / 256 / 1024
    amortization     1 / 1.5 / 7 / 29 / 115x;   broad_batches 20000/2500/313/79/20;  broad_qs_eval 0
    broad_batch tests 7/7 pass;  batch == scalar per-title for all bs;  Inline == Columnar (kill-switch)
  THROUGHPUT — indicative, 16 threads, bench 1M 20k 0.05:
    inline broad (per-title)     ~760k titles/s    (the naive status quo — broad re-scanned per title)
    selective via batch path     ~2.5M titles/s    (broad OFF; coarse par_chunks granularity alone helps)
    columnar batch broad bs=256  ~1.8M titles/s    (2.4x over inline broad;  broad lane +37% over selective)
    sweep throughput peaks near bs=256 (419k bs=1 -> 1.8M bs=256 -> 1.7M bs=1024)

[2026-05-30]  Apple M4 Max, 16-core (12P+4E), 48 GiB, arm64.  Verification pass after the
              /_cat/segments work (ADR-023).  Loaded interactive box — throughput is indicative.
  INVARIANTS — all reproduced:
    segbench cands/title 53.48 / 55.11 / 56.67 / 57.68  (EXACT match to reference); skip 46/63/76/86%
    bench 1M selective:  54.52 cands/title (p95 96, p99 112);  postings==cands;  matches 0
    cost classes @5% broad:  A 995660 / B 2116 / C 2224 / D 0
    resident @1M:  retain_source=false 5.9 B/q;  sources 113.5 B/q;  dict 4.9 B/q;  alive 1.0 B/q
    stress 15/15 pass;  10M soak: 0 ghosts, par==seq 0 mismatches, compact 4->1 reclaimed 550k
  THROUGHPUT — indicative (single core unless noted):
    build ~510–535k q/s;  selective match ~590–700k titles/s/core;  +broad inline ~184k
    updates ~205–825k/s;  snapshot() publish 52M/s;  PUT+publish 488k/s;  bulk(1k)+publish 733k q/s
    parallel selective, 16 threads ~1.0M titles/s total (~1.7–2.6x single-thread — bandwidth-bound)
    10M mixed-ops full cycle (insert/delete/update/flush/compact/search): 30.3s

[2026-05-27]  aarch64, 4 cores, 3.8 GiB, rustc 1.95.0 (release: opt-level=3, lto, cgu=1).
              ORIGINAL REFERENCE.  NOTE: results.md §1's headline throughput table predates the
              daachorse/roaring/rayon swap-ins; the structural numbers below + segbench are
              current-engine and reproduce today (see 2026-05-30).
  bench 1M  broad=0    : build 1.47s (679k q/s); 54.64 cands/title (p95 96, p99 112); 735k titles/s/core; RSS 258 B/q
  bench 5M  broad=0    : build 7.55s (662k q/s); 54.29 cands/title;                    455k titles/s/core; RSS 258 B/q
  bench 1M  broad=.05  : 682 cands/title (broad-inline);  selective 234k vs broad-inline 67k titles/s/core (~3.5x)
  bench 1M  broad,skew3.5: 670 cands/title;  selective 285k titles/s/core  (hot-entity skew tolerated)
  segbench             : 53.48/55.11/56.67/57.68 cands/title;  717728/574005/327257/351379 titles/s/core
  oracle               : 109024 == 109024 matches, false_neg=0, false_pos=0
  updates              : ~730–825k/s/core, immediate (epoch) visibility

============================================================================================
APPENDIX — NPMI corpus-feature learner capture (2026-05-27, 4-core; preserved here as the only
recorded copy of this one-time run).  Reproduce: learn 500000 50 0.30.  This is a
one-time demonstration that every anchor the matcher needs is derivable from query
co-occurrence statistics — not a recurring benchmark.
============================================================================================
corpus: 500000 queries, learning with no hand-coded vocabulary
params: min_count=50, npmi_tau=0.3

=== iteration 1: discovered 89 multi-token entities (bigrams) ===
entity (by frequency)              count     npmi
psa_8                              86317    0.736
upper_deck                         63696    1.073
psa_9                              35917    0.592
bgs_8                              35206    0.589
psa_9.5                            27369    0.558
sgc_8                              27341    0.560
psa_10                             24115    0.547
base_psa                          17449    0.415
insert_psa                        17396    0.414
rc_psa                            17263    0.411
sp_psa                            17194    0.411
preview_psa                       17190    0.412
refractor_psa                     17146    0.412
rookie_psa                        17036    0.406
bgs_9                             14862    0.497
bgs_9.5                           11471    0.476
sgc_9                             11311    0.472
bgs_10                             9614    0.456

--- of which 30 are name-like (all-alphabetic), top by binding strength (npmi): ---
entity                             count     npmi
upper_deck                         63696    1.073
michael_jordan                      4239    1.043
lebron_james                        1811    1.038
kobe_bryant                         1402    1.037
ken_griffey                         1162    1.036
wayne_gretzky                       1002    1.035
tom_brady                            986    1.035
mike_trout                           858    1.035
patrick_mahomes                      816    1.034
base_psa                           17449    0.415
insert_psa                         17396    0.414
preview_psa                        17190    0.412

=== iteration 2: discovered 101 longer entities (trigram+) ===
entity (by frequency)              count     npmi
base_psa_8                          8791    0.510
insert_psa_8                        8673    0.507
sp_psa_8                            8662    0.508
rc_psa_8                            8649    0.507
preview_psa_8                       8642    0.508
refractor_psa_8                     8609    0.507
rookie_psa_8                        8343    0.500
base_psa_9                          3639    0.441
insert_psa_9                        3617    0.440
rookie_psa_9                        3605    0.442

=== selectivity gain: learned phrase vs its parts (lower df = better anchor) ===
learned entity               df(phrase)   min df(part)     gain x
psa_8                             86317         148864       1.7x
upper_deck                        63696          63696       1.0x
psa_9                             35917          62090       1.7x
bgs_8                             35206          71153       2.0x
psa_9.5                           27369          47476       1.7x
sgc_8                             27341          54651       2.0x
psa_10                            24115          41092       1.7x
base_psa                          17449          49819       2.9x
insert_psa                        17396          49783       2.9x
rc_psa                            17263          49985       2.9x
sp_psa                            17194          49739       2.9x
preview_psa                       17190          49386       2.9x
refractor_psa                     17146          49367       2.9x
rookie_psa                        17036          50408       3.0x

=== learned feature universe (no hand-coded vocab) ===
distinct unigrams in corpus : 17196
entities learned (iter 1)   : 89
entities learned (iter 2)   : 101

Takeaway: every anchor the matcher needs is derivable from query co-occurrence statistics.
The learner glues multi-token entities (raising selectivity) and the signature optimizer
already ranks anchors purely by frequency — no taxonomy required.
