# Architecture Decision Records

Lightweight record of the key design decisions in Reverse Rusty. Each entry captures the context,
the decision, and the consequences — so that future contributors (human or agent) understand
*why* things are the way they are, not just *what* they are.

**This file is the index.** Each ADR's full text lives in its own file under
[`decisions/`](decisions/) (`decisions/adr-NNN-slug.md`); the tables below give a one-line summary and
link to each. To find a decision, scan the relevant theme below, then open the one file — you should
never have to read all of them. (Implementation status of each decision is tracked in
[`STATUS.md`](STATUS.md), not here.)

ADRs are **append-only and never renumbered** — a superseded or reversed decision is marked in place,
not deleted (the record of *why something was not done* is as load-bearing as the rest).

> **Adding an ADR:** create a new `decisions/adr-NNN-slug.md` (next free number, a short kebab-case
> slug) and add one row to the matching theme table below. Never renumber, rewrite, or delete an
> existing record; supersede it in place and note the superseding ADR.

## Matching & verification

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [001](decisions/adr-001-semantic-signatures.md) | Semantic signatures over term-level gating | Gate candidates on 2–3 *semantic* feature combinations from a domain-aware normalizer, not raw terms → flat ~54 candidates/title at any corpus size. | Accepted |
| [002](decisions/adr-002-integer-exact-verification.md) | Integer-only exact verification | Push all parsing/AST work to compile time; the match hot path is pure `u64`-mask + sorted-`u32` work — no strings/regex/alloc. | Accepted |
| [003](decisions/adr-003-broad-query-quarantine.md) | Broad-query quarantine via cost classes | Classify queries A–D at compile time; route non-selective class C to a batch lane, reject unconstrained class D — keep the selective path fast. | Accepted |
| [006](decisions/adr-006-forbidden-features-never-gate.md) | Forbidden features never gate (structural) | MUST_NOT features are invisible to the signature optimizer and checked only in exact verification — gating on an absent feature would be a false negative. | Accepted |
| [011](decisions/adr-011-cache-line-blocked-bloom.md) | Cache-line blocked bloom skip-filter | Per-segment anchor skip-filter is a 512-bit cache-line blocked bloom (1 memory access), chosen over binary-fuse/u64-blocked to fit the probe budget. | Accepted |
| [019](decisions/adr-019-query-family-factoring-declined.md) | Query-family factoring evaluated and declined | Declined the shared-prefix/family DAG — it optimizes a non-bottleneck at high format/rebuild cost; implicit anchor-sharing already prunes near-duplicates. Reversible. | **Declined** |
| [025](decisions/adr-025-query-complexity-limits.md) | Wire query-complexity limits into the parser | Thread configured max length/clauses/any-of-size into the parser so the flags + `/_settings` actually enforce; WAL replay keeps the compiled-in ceiling. | Accepted |
| [026](decisions/adr-026-broad-lane-batch-evaluation.md) | Broad-lane batch / columnar evaluation | Evaluate the broad lane once per title-batch via columnar bitmap algebra (`/_mpercolate`); byte-identical to per-title, removes the broad bottleneck. | Accepted |

## Normalization & vocabulary

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [010](decisions/adr-010-normalizer-builder-fallible.md) | NormalizerBuilder + fallible construction | Replace hardcoded vocab + `.expect()` with a fluent `NormalizerBuilder` + fallible `default_vocab()`; domain-agnostic, zero panics, Debug/Send/Sync everywhere. | Accepted |
| [015](decisions/adr-015-runtime-vocabulary-learning.md) | Runtime vocabulary learning from any-of groups | Learn synonyms from query any-of groups at runtime (`Vocab` + `/_vocab`); a `vocab_epoch` counter tracks segments compiled under a now-stale normalizer. | Accepted |
| [046](decisions/adr-046-dynamic-vocabulary.md) | Dynamic vocabulary (Cluster v1) | Absorb terms after the dict is frozen — feature-hashing for unknown tokens (no coordination, bounded FP, never FN) + runtime normalizer learning for aliases (blue/green rebuild). Built. | Accepted |
| [053](decisions/adr-053-corpus-phrase-vocab-source.md) | NPMI corpus phrase induction as a runtime vocab source | Wire the `learn` binary's NPMI collocation miner into a library `corpus::learn_phrases_from_text` → `Vocab`, composed under the ADR-015 any-of learner via an opt-in `CorpusLearnConfig`/`learn_and_apply_with`. Phrases only (no aliases) ⇒ same-normalizer gluing ⇒ oracle-equivalent, zero FN; default-off ⇒ byte-identical. | Accepted |
| [054](decisions/adr-054-equivalence-expansion.md) | Equivalence (alias) learning via expansion, not collapse | First-class `Vocab.equivalences` + a compile-time `Extracted::expand_equivalences` that widens a required feature into an any-of over its group (query-side, via a transient `dict::EquivMap`). Structurally FN-safe (match set only grows; wrong alias ⇒ bounded FP). Declared + any-of-learned sources (opt-in); distributional/match-feedback discovery deferred behind the same seam. | Accepted |
| [058](decisions/adr-058-punctuation-equivalence-folding.md) | Configurable punctuation-equivalence folding | Make byte-cleaning's per-character behavior a configurable `PunctClass` table (`Split`/`Fold`/`Keep`/`Marker`) on the shared normalizer; declaring `'`/`-` as `Fold` collapses `O'Brien`/`O-Brien`/`OBrien` to one token, closing a recall gap. Same table over queries + titles ⇒ cover holds. Default reproduces the historical behavior (byte-identical); opt-in, persisted via `Vocab`. | Accepted |

## Ingestion, storage & durability

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [004](decisions/adr-004-lsm-write-path.md) | LSM write path over full rebuild | Log-structured writes (immutable segments + memtable + tombstones, atomic epoch swap) instead of full rebuild — ~750k updates/sec/core, immediate visibility. | Accepted |
| [009](decisions/adr-009-score-based-compaction.md) | ClickHouse-style score-based compaction | Score-based greedy compaction (not RocksDB leveled) because percolation must probe *every* segment — directly minimizes time-integrated segment count. | Accepted |
| [012](decisions/adr-012-mmap-segment-format.md) | mmap'd segment file format + frozen hash tables | Custom `.seg` format with frozen open-addressing hash tables, mmap'd for zero-copy reads; the precondition that makes the anchor skip-filter pay off. | Accepted |
| [013](decisions/adr-013-write-ahead-log.md) | Write-ahead log (WAL) for crash recovery | Append-only CRC-framed WAL for the memtable; WAL-first (durability before visibility), a failed append rejects the write, fsync policy configurable. | Accepted |
| [014](decisions/adr-014-query-source-store.md) | Engine-level query source store | Keep original query text in an engine-level `sources.dat` (not in `.seg`), so source text never touches the match hot path. | Accepted |
| [016](decisions/adr-016-snapshot-read-path-arcswap.md) | Snapshot read path (ArcSwap) over RwLock | Lock-free reads via `ArcSwap<EngineSnapshot>` + write-only `Mutex`; structural sharing makes publish O(1) (PUT 82 ms → ~2 µs at 1M queries). | Accepted |
| [017](decisions/adr-017-durable-bulk-ingest.md) | Durable bulk ingest (segment = artifact, manifest = commit) | Bulk ingest is durable-or-rejected (RocksDB IngestExternalFile model); no silent in-memory fallback, manifest write is the atomic commit point. | Accepted |
| [018](decisions/adr-018-bulk-ingest-per-item-outcomes.md) | Bulk ingest reports per-item outcomes (ES-style) | `/_bulk` returns per-item statuses (which queries were dropped + why), not just an aggregate count. | Accepted |
| [020](decisions/adr-020-resident-memory-reduction.md) | Production-scale resident-memory reduction | Lazy on-disk source store + flat logical-index columns cut resident memory ~148 → ~96 B/query (→ ~4.5 with both, opt-in) ahead of sharding. | Accepted |
| [051](decisions/adr-051-fail-closed-flush-compaction.md) | Fail-closed flush, compaction & reseal | Extend ADR-017's durable-or-rejected discipline to flush/compaction/reseal/recompile: build the replacement durable before destroying what it replaces, gate WAL-advance/file-deletion on the commit point. No silent restart data loss on disk failure. | Accepted |
| [056](decisions/adr-056-compaction-reanchoring.md) | Compaction-that-improves (re-anchoring drifted queries) | Opt-in `compaction_reanchor`: a merge re-derives each alive query's cover with current frequencies (decoding the stored SoA, reusing `anchor_plan`) so a drifted anchor moves to a more-selective one — shrinking hot postings + fan-out. FN-safe by the `anchor_plan`/`match_into` matched pair (SoA copied verbatim); a no-op in a cluster (frozen dict); default-off ⇒ byte-identical. Closes the Tier-2 item. | Accepted |
| [057](decisions/adr-057-frozen-dict-format-versioning.md) | Version + harden the frozen-space serializations (feature dict + tag dict) | Add a `magic + version` header (`RDCT`/`RTGD`) to the two previously-unversioned binary formats so a layout change or newer-build blob fails loud instead of misparsing; strict `dict::kind_from_tag` decode (unknown tag ⇒ error, never silent `Generic`); fully-fallible parse (no panic on truncation). Legacy header-less blobs still read; content-based fingerprint untouched ⇒ gRPC handshake byte-identical. Closes the "dict format not versioned" robustness item. | Accepted |
| [066](decisions/adr-066-tombstone-durability-at-commit.md) | Tombstone durability at the commit point | Fixes two pre-existing single-node bugs found building the ADR-064 upsert: (1) base-segment deletes lived only in the in-RAM mmap overlay + their WAL frames, so the flush-time WAL reset **resurrected** acknowledged deletes on reopen (default-config reproducible); (2) compaction renumbers `(seg_idx, local)` while stale positional WAL frames remain, so a crash replay could **tombstone the wrong query** — a zero-FN violation. Fix: per-segment dead-locals roaring bitmaps in Manifest **v3** (the Lucene `.liv` analogue), applied on open before WAL replay; the production delete logs ONE address-free `DeleteByLogical` frame (WAL **v3**) replayed through the live path's funnel; a manifest `wal_seq_watermark` skips stale base-positional frames. v1/v2 manifests + v1/v2 WALs read back unchanged. | Accepted |

## Engine, errors, dependencies & ops

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [005](decisions/adr-005-typed-errors.md) | Typed errors over stringly-typed Results | `ParseError { kind, pos }` + `IngestReport` instead of `Result<_, String>` and silent drops — inspectable errors; no `unwrap()` in library code. | Accepted |
| [007](decisions/adr-007-three-production-dependencies.md) | Three production dependencies | Adopt daachorse / roaring / rayon for alias matching, large postings, and data-parallel matching once the std-only design was validated. | Accepted |
| [008](decisions/adr-008-deterministic-data-generation.md) | Deterministic data generation (seeded PRNG) | Seeded SplitMix64 PRNG (no crates) so benchmarks + the oracle are reproducible and adversarial patterns are configurable parameters. | Accepted |
| [021](decisions/adr-021-durability-failures-observable.md) | Durability failures are observable events | Route the ~14 durability-failure sites through a structured `EngineEvent::DurabilityFailure` (op + severity), not stderr — operators alert from metrics/logs. | Accepted |
| [022](decisions/adr-022-runtime-settings-api.md) | ES-style runtime settings API (`/_settings`) | `GET/PUT /_settings` reads the live config lock-free and updates the dynamic subset at runtime with all-or-nothing per-key validation; static keys rejected. | Accepted |
| [023](decisions/adr-023-per-segment-introspection.md) | Per-segment introspection (`/_cat/segments`) | Expose per-segment holes / memory-split / staleness (text or JSON), read lock-free from the snapshot. | Accepted |
| [024](decisions/adr-024-ci-github-actions.md) | CI via GitHub Actions mirroring `check.sh` | CI runs `check.sh` itself (one source of truth); commit the pressure suite + benchmark baseline; benchmarks run-and-print, never gate (hardware variance). | Accepted |
| [028](decisions/adr-028-lean-core-feature-gate.md) | Feature-gate the server stack (lean core) | Gate the server/observability stack behind a default-on `server` feature (Cargo-level, zero `#[cfg]`); a lean-core clippy lane keeps server crates out of library code. | Accepted |
| [050](decisions/adr-050-golden-front-end-tests.md) | Oracle front end pinned by spec-authored golden tests | The differential oracle shares the engine's parse/normalize/extract front end (and runs empty-vocab), so a front-end bug would hide; pin those three stages with hand-authored golden tests + a vocab-rich oracle pass. | Accepted |
| [052](decisions/adr-052-external-review-hardening.md) | External-review hardening (batch) | Six small review fixes: reject `-` + space in the parser; reserve 0 in `sig_key` (frozen-table sentinel); apply `max_percolate_batch` to multi-doc `/_search`; bounds-validate segment sections before the unsafe cast; document `timeout_ms` as response-only; default the HTTP bind to `127.0.0.1` + `--host`. | Accepted |

## Clustering — core & transport

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [027](decisions/adr-027-in-process-multi-shard-core.md) | In-process multi-shard core | K-shard coordinator: one shared frozen dict → globally-stable `FeatureId`s, a feature-anchor ring (~2–5 fan-out), broad lane on a replicated shard. The no-false-negative heart of clustering. | Accepted |
| [029](decisions/adr-029-grpc-shardserver-shard-seam.md) | gRPC `ShardServer` + local↔remote `trait Shard` | Lift the shard behind a `trait Shard` + a tonic `ShardServer` (off-by-default `distributed`); ships DSL not feature-ids; the fallible seam preserves zero-FN. | Accepted |
| [030](decisions/adr-030-dict-fingerprint-handshake.md) | Dict-fingerprint handshake + fallible construction | Connect-time `Dict::fingerprint` handshake turns a divergent cross-process dict from a silent false-negative into a loud `DictMismatch`; construction made fully fallible. | Accepted |
| [031](decisions/adr-031-externalized-coordinator-log.md) | Externalized coordinator log (`trait ClusterLog`) | A durable CRC-framed (+ null) ordered, log-first mutation log so the whole cluster is rebuildable from the log alone. | Accepted |
| [032](decisions/adr-032-per-shard-durable-segments.md) | Per-shard durable compiled segments | Reopen by attach-and-mmap per-shard `.seg` files (not re-ingest); coordinator manifest is the atomic commit point; checkpoint re-seals tombstoned base segments. | Accepted |
| [033](decisions/adr-033-shared-nothing-storage.md) | Shared-nothing cluster storage | Supersede the Aurora/object-store framing — shared-nothing (local segments + per-node WAL + peer recovery + Raft control plane), like ES/Cassandra/Kafka. | Accepted |
| [034](decisions/adr-034-cross-process-dict-shipping.md) | Cross-process dict shipping over gRPC | Ship the frozen dict to each server at connect (`AdoptDict`); a data node starts empty/pending instead of rebuilding the dict from the whole corpus. | Accepted |

## Clustering — replication & control plane

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [035](decisions/adr-035-per-shard-replication-peer-recovery.md) | Per-shard replication + peer recovery (`ReplicatedShard`) | One primary + N replicas (in-process): primary-authoritative writes, in-sync-only read failover, peer recovery by streaming segments. Set-equality is the basis. | Accepted |
| [036](decisions/adr-036-grpc-replication-peer-recovery.md) | gRPC multi-node replication + peer recovery | Lift replication + peer recovery onto gRPC — remote replicas that fail over + cross-node segment streaming (`FetchSegments`/`RecoverFrom`); servers become durable. | Accepted |
| [037](decisions/adr-037-control-plane-seam.md) | Control-plane seam (`trait ControlPlane`) | Dependency-free seam + in-memory backend holding the cluster-state doc (ring params + shard→node map + membership + epoch); shaped for openraft, byte-identical by default. | Accepted |
| [038](decisions/adr-038-openraft-control-service.md) | openraft backend + gRPC `ControlService` | A real openraft backend behind the seam; consensus holds only the cluster-state doc, never query mutations; survives leader death. | Accepted |
| [039](decisions/adr-039-durable-translog-no-quiesce-recovery.md) | Durable translog + no-quiesce peer recovery | Per-shard durable+replicated query log lets recovery stream segments at P then replay the tail > P — recovery without quiescing writes; data nodes self-restart. | Accepted |
| [040](decisions/adr-040-translog-retention-leases.md) | Translog retention leases + finalize | Leases pin the translog tail across a recovery (min over holders) so a concurrent seal can't strand it; a bounded finalize loop grows a replica in-sync without pausing writes. | Accepted |
| [041](decisions/adr-041-durable-raft-log-recovery.md) | Durable Raft log + control-plane restart recovery | Make the openraft log/vote/committed/snapshot durable so a `controlserver --data-dir` survives a crash and rejoins quorum; `apply` stays pure in-memory. | Accepted |

## Clustering — elasticity & repair

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [042](decisions/adr-042-shard-node-allocator.md) | Shard→node allocator (rendezvous hashing) | HRW hashing computes a balanced, minimal-movement shard→node map; `rebalance` commits only changed positions; in-process the map is advisory. | Accepted |
| [043](decisions/adr-043-swappable-shard-backing.md) | Swappable shard backing (`HandoffShard`) | Make a position's backing atomically swappable (`ArcSwap` + generation fence stamp), serve-then-drop for free — the routing-flip half of a live handoff. | Accepted |
| [044](decisions/adr-044-live-data-moving-handoff.md) | Live data-moving handoff | `execute_handoff` wires decide→move→flip: no-quiesce bulk recover → fence the source (writes only) → drain to convergence → flip routing. A shard moves owners live, zero FN. | Accepted |
| [045](decisions/adr-045-autoscaler.md) | Autoscaler (policy/trigger over rebalance) | A pure `evaluate` policy (membership→rebalance; skew→handoff advisory; corpus→split advisory) + a `tick` driver; idempotence is the hysteresis; disabled by default. | Accepted |
| [047](decisions/adr-047-remote-partial-apply-resync.md) | Remote live-write partial-apply repair (`resync`) | A mid-fan-out remote write failure is detected (typed `PartiallyApplied` + event) and repaired live (`resync`) instead of silently partial; + a safe `block_on` thread-context contract. | Accepted |
| [048](decisions/adr-048-reliability-hardening.md) | Reliability hardening | Auto-unfence-on-abort (`Unfence` CAS), translog-lease TTL reap, and wiring the autoscaler's `Handoff` advisory through to `execute_handoff`. | Accepted |
| [065](decisions/adr-065-distributed-v1-graduation.md) | Distributed v1 — graduation criteria (experimental → release-candidate) | A program ADR defining the milestone that retires the "experimental" label: feature-complete + ready for full-feature **multi-machine** testing (not production-proven). The checklist: a cluster REST surface, TLS/auth on the gRPC transports, a real multi-machine harness, tagged-cluster vocab change, cluster ranking, cross-process vocab shipping, auto-split, replicate-broad-to-all (or decide), tag-dict recovery fingerprint, packaging + runbook, backup/restore, and a ≥20M multi-shard scale proof. Each criterion ships under its own ADR/PR. | Accepted |

## Percolator parity

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [049](decisions/adr-049-percolator-parity-tags.md) | Per-query metadata, filtered percolation, ranking | Per-query integer metadata tags in the SoA + filtered percolation pushed into verify (never gating, mirroring ADR-006) + optional out-of-core ranking. Built single-node + oracle-proven. | Accepted |
| [055](decisions/adr-055-cluster-tags-filtered-percolation.md) | Tags + filtered percolation through the cluster | Thread tags end-to-end (in-process + gRPC): one shared frozen `TagDict` (like the `Dict`), raw tags in the log + read-only `get_or_synthetic` resolution (never `intern`), filter resolved once + shipped as `TagId` groups, tag-dict shipping + fingerprint handshake. Additive APIs ⇒ untagged path byte-identical. Built + oracle-proven. | Accepted |
| [059](decisions/adr-059-percolate-ranking-pagination.md) | Percolate ranking + pagination (ADR-049 dp-4, single-node) | Opt-in post-match ranking: a new `rank.rs` scorer + `EngineSnapshot::rank` order the already-final id set by `Σ boosts + priority-tag value` (additive), tie-broken by `_id`; the handler sorts + applies `from`/`size` + emits `_score`. Adds `from` to `/_mpercolate` and per-slot truncation (closes the ADR-052 #3 tail). Touches neither index nor verifier ⇒ zero-FN; default byte-identical. Cluster ranking still deferred. Built + tested. | Accepted |
| [060](decisions/adr-060-learned-alias-evolution.md) | Learned-alias evolution — Phase 1 (safe single-token activation) | A governing `AliasRegistry` (provenance / kind / confidence / status) over ADR-054 expansion: a structural classifier auto-activates only single-token spelling/abbreviation variants; learned category alternatives `(psa,bgs,sgc)`, multi-word, and mixed-kind groups stay review candidates. Solr/Lucene import + group-level any-of learning + the **alias-ID-stability fix** (intern active forms before resolving, so a later insert can't flip a synthetic→dense id and silently kill the alias). Active groups feed `effective_equivalence_groups`; live apply via `set_vocab`+recompile; REST `GET/POST /_vocab/aliases*`. Single-node; multi-word = Phase 2. Zero-FN; default byte-identical. Built + oracle-proven. | Accepted |
| [061](decisions/adr-061-token-graph-multiword-aliases.md) | Token-graph multi-word aliases — Phase 2 (positive/negative title feature views) | Activates the multi-word candidates ADR-060 recorded. The wall: a title emits ONE feature set used for both required and forbidden checks, but multi-word retrieval needs the overlapping superset (unsafe for negation). Fix: **two title-side feature views** — positive superset `P(T)` (overlap-aware retrieval + required + any-of) and canonical leftmost-longest `N(T)` (forbidden only), threaded as `TitleView` through `verify`/`match_into`. Query side collapses an alias phrase to its entity (ADR-054 expansion widens it); title side is additive + overlapping. The equivalence machinery is reused unchanged (a collapsed form resolves to one entity). Forbidden policy = canonical leftmost-longest (recall-safe). Multi-word now auto-activates when declared/manual. Single-node; broad lane routes to the two-view inline path while aliases active. Zero-FN (oracle incl. forbidden-over-multi-word from day one); default byte-identical. Built + oracle-proven. | Accepted |
| [062](decisions/adr-062-server-bearer-auth.md) | Opt-in bearer-token auth for the HTTP server's mutating/admin endpoints | The deferred ADR-052 hardening item: a single static token (`--auth-token`/`RR_AUTH_TOKEN`) enforced by one middleware (`bin/server/auth.rs`). **Default-deny** — every non-GET/HEAD request needs `Authorization: Bearer <token>` except the POST-read percolate endpoints (`/_search`, `/_mpercolate`), so a future mutating endpoint fails closed; `--auth-protect-reads` extends the gate to reads (all but `/_health`). Constant-time compare, RFC 6750 401 challenge + ES-style `security_exception` envelope, `auth_failures_total{reason}`, fail-loud config resolution, non-loopback-without-auth startup warning. Unset ⇒ byte-identical (strictly opt-in). Meilisearch/Qdrant-class single-token shape, zero new deps; TLS + gRPC auth stay Tier-3. Built + tested (unit, router-level, live). | Accepted |
| [063](decisions/adr-063-adversarial-test-hardening.md) | Adversarial test hardening — messy corpora, reference-free properties, boundary pins | A test-escape post-mortem (every external-review-caught bug had passed a green suite) traced the escapes to too-clean generated data + the differential oracle's shared front end (ADR-050). Added: seeded **messy mode** on the generator (opt-in `messify_*`; every clean corpus byte-identical) + messy differential passes (per-title + batch); a **reference-free property suite** (`tests/adversarial/` — the self-match diagonal, metamorphic set-identity under surface noise, ADR-054/058/060/061 cross-form matrices incl. the codex-R11 whitespace-run regression, unicode-soup fuzz pinning `P(T) ⊇ N(T)` + `match_features == N(T)` + no-panic); a degenerate-input differential; posting-tier boundary units (8→9, 256→257); a WAL mid-file bit-flip pin — which **immediately caught + fixed** a real `skipped_bytes` under-count — and a delete-twice/reinsert pin. Purely additive; no hot-path change. Built + passing. | Accepted |
| [064](decisions/adr-064-percolator-drop-in-parity-audit.md) | Drop-in percolator parity — audit findings + work package | A program ADR from a 2026-06 drop-in-replacement audit against the reference percolator workload (`research/percolator-workload.md`), including an **empirical parity PoC** — real stored queries translated under a documented parity configuration, ground truth computed by executing the reference deployment's own precision matcher: **zero FN on every pinned accepting pair**, every FP predicted. The decided work package: atomic-upsert `PUT` (✅ shipped, ADR-067), an opt-in **class-D always-candidate lane** (ES/OS treats pure-negative queries as match-all-except; ✅ shipped, ADR-068), a parity-mode normalizer knob (the `pop` year-demotion is the one residual FN class), loud non-string tag-value handling, wiring `maybe_flush` into the REST PUT path, per-request `include_broad` on `/_search`, + backlog items (restart-time measurement, backup procedure, tag read-back). | Accepted |
| [067](decisions/adr-067-atomic-upsert-put.md) | Atomic upsert — `PUT /_doc/{id}` is replace-by-id (ES `index` semantics) | ADR-064 item 1: the re-PUT divergence (a second live copy; the id matched under either version until DELETE; `deleted_count: 2`) closed by an atomic upsert — insert the new version + tombstone every prior live copy under ONE writer critical section, ONE WAL frame (`Upsert`, op 4, WAL **v4**), ONE snapshot publish; 201-created / 200-updated. A failed replace (parse / class-D / WAL) never deletes — capture-before-insert, tombstone-after-acceptance. Replay splits by state domain over the ADR-066 watermark (insert half always; memtable priors always; segment priors only above it). Also fixed along the way: `Engine::open`'s fresh (no-manifest) path now replays the WAL tail — a start-empty server no longer silently loses every acknowledged write on its first crash (completes ADR-013's stated contract). Single-node; cluster upsert deferred to ADR-065. Built + tested. | Accepted |
| [068](decisions/adr-068-class-d-always-candidate-lane.md) | The class-D always-candidate lane (opt-in accept-and-quarantine for negation-only queries) | ADR-064 item 2: an opt-in lane (`accept_class_d`, default off = today's loud reject) storing a negation-only query as an **always-candidate** — `anchor_plan` derives the cover of an empty positive set as ONE empty broad-anchor group (the **universal signature**, `sig_key(&[])`), probed once per segment / once per batch in the broad lane; forbidden features enforced only in exact verification (`is_pure_anchor` structurally false for class D ⇒ verify can never be skipped). The ES/OS `fixNegativeQueryIfNeeded` match-all-except + Lucene failed-extraction-bucket parity. The live write paths gate BEFORE the WAL; accepted class-D writes carry their own op codes (`InsertClassD`/`UpsertClassD`, WAL **v5**) so replay reproduces each frame's WRITER decision — legacy logged-before-classifying frames keep the old reject gate (a codex catch: replaying a legacy rejected upsert as accepted would tombstone the acknowledged-live prior version, an FN) — and a class-D-bearing commit writes segment + manifest format **v4** (layout-identical) — the manifest is the loud rollback gate (old recovery merely skips an unreadable segment; an unsupported manifest version fails `Engine::open` outright). The vocab recompile keeps stored class-D entries (closing a pre-existing silent-drop-to-unmatchable hazard); the effectively empty query (no positives, no forbidden) rejects regardless. `class_counts()[3]` now counts STORED class-D (rejections stay the separate `rejected_class_d` metric). The cluster keeps rejecting at placement — the cluster lane rides the ADR-065 replicate-broad-to-all criterion. Default byte-identical; oracle-proven (the vacuous-accept differential: per-title + batch ≡ brute, tombstones/compaction-both-variants, flush→mmap reopen + WAL replay under a flipped knob, vocab-change survival). | Accepted |
| [069](decisions/adr-069-parity-number-context-words.md) | Parity-mode number typing — the `pop` demotion becomes a configurable number-context word list | ADR-064 item 3: the hard-coded `pop` number-context rule (a number right after `pop` demotes to `term:N`, never `year:N` — position-sensitive typing, the audit's ONE residual FN class, demonstrated in both directions) generalizes to a **number-context word list** on the shared normalizer — the ADR-058 move (hard-coded → configuration), not a boolean. Default `["pop"]` = byte-identical; an **empty list disables the rule** (the parity mode: a 4-digit year is `year:N` everywhere); custom lists work (`["qty"]`). Persisted as `Vocab.number_context: Option<Vec<String>>` (`None` = default, old JSON untouched) — applies live via the `PUT /_vocab` `set_vocab` recompile (flip-proven end-to-end, reversibly) and survives reopen through the standard vocab channels (`--vocab-file`/`open_with_vocab` single-node, the `ClusterManifest` vocab blob). The `#`/`/` context rules already ride the ADR-058 punctuation table, so after this EVERY number-context rule is configuration-controlled. The ADR-064 "emit both typings title-side" variant **evaluated + declined**: the shared normalizer + vocab-epoch recompile make mixed typing unrepresentable (nothing left to rescue), and both-typings is NOT FP-only in the single-view path (it flips forbidden-year outcomes under a `Marker` `#`) unless forced through the ADR-061 dual-view — a hot-path cost for an empty case. Trade pinned: with graders configured, disabling the rule makes `psa pop 7` read as a grade (the parity config has no graders). Oracle-proven (engine ≡ brute under the knob incl. forbidden/any-of; both audit directions closed + default contrast). | Accepted |
| [073](decisions/adr-073-rest-parity-hardening.md) | REST surface honesty — tag-value coercion, flush wiring, per-request broad | ADR-064 items **4–6** in one hardening batch (the ADR-052 precedent): **(4)** non-string tag values — silently dropped on ingest and in filter arrays (corrupting filtered percolation invisibly: the query became unreachable by any filter on that key) — now take **canonical scalar coercion** (number/bool → JSON text, the ES keyword behavior; ONE shared function on ingest + both filter parsers so the sides can never disagree; `7.0` → `"7.0"` pinned as in ES), null = the ES "no value" (skipped on ingest, **400 in a filter** — an unanswerable predicate never silently widens), objects/nested arrays/non-object `tags` = loud 400 everywhere (`/_bulk` per-item; cluster mode shares the same helpers). Coerce-over-reject because the reference workload's dominant filter key is a numeric category — a drop-in that 400s payloads the reference accepted is not a drop-in. **(5)** `memtable_flush_threshold` was inert for REST single-doc writes (`put_doc` bypassed the only `maybe_flush` site) — the knob now lives at the success tail of BOTH fallible live-write paths (never on rejected writes; replay/bulk/cluster funnels deliberately untouched). **(6)** `/_search` gains the per-request `include_broad` override `/_mpercolate` + the cluster handlers already had (the body field was silently swallowed); `deny_unknown_fields` declined — sibling-tag ingest depends on tolerance. Nothing touches signature gating; absent/unset ⇒ byte-identical. Handler-proven incl. the ingest-meets-filter agreement. | Accepted |

## Distributed v1 — the ADR-065 graduation program

| ADR | Decision | Summary | Status |
|---|---|---|---|
| [070](decisions/adr-070-cluster-rest-surface.md) | Cluster REST surface — the coordinator-mode server | ADR-065 criterion 1 (the first of the three that unblock testing everything else): a **coordinator mode** inside the existing `server` binary (`--cluster`) — one REST dialect, two backends, shared auth/request-id/metrics middleware (a `RequestCtx` seam). In-process (`--shards K`, durable build/reopen via `cluster_exists` → `open`) on the default build; remote (`--shard-endpoint primary[,replica…]`, dict + tag space minted by the extracted `freeze_feature_space` pass A and shipped at connect) under `distributed`. Concurrency: `RwLock<ClusterEngine>` — reads AND ordinary writes on the read side (`&self`, log-ordered; a `write_serial` mutex serializes request-level writes), write side only for the `&mut self` vocab rebuilds. `PUT /_doc` becomes a **cluster-atomic upsert**: a new single-frame `ClusterMutation::Upsert` (clog **v3**, op 2 — two frames would lose the query if a crash split a Remove+Add pair); `apply_upsert` places FIRST (a rejected new version never deletes, ADR-067 parity), then tombstones everywhere + inserts on the placement shards, live ≡ replay; partial failures ride the ADR-047 repair queue with the Upsert as the queued mutation. Honest deltas: no-cluster-analogue surfaces answer **501 with the alternative**, unsupported request features (`rank` → criterion 5, `explain`) are **400s never silently ignored**, `GET /_doc` works on local clusters via a new `Shard::source_of` (default = loud error, so a remote shard can't fake "not found"). Vocab/alias admin maps onto `set_vocab` + new cluster-level `import_alias_synonyms`/`learn_aliases_and_apply`/`learn_vocab`; every refusal (non-local/tagged/multi-word) surfaces verbatim. Remote coordinator is stateless (durability = the shard nodes' translogs; deterministic dict re-mint keeps the ADR-034 handshake green across coordinator restarts). Proven: clog round-trip/torn-tail, coordinator upsert units (created/updated/moved/never-deletes/fail-closed), the durability oracle's upsert module (log-tail AND checkpoint reopen ≡ pre-crash ≡ brute), and cluster handler tests over a real in-process cluster (CRUD/bulk/filtered search/include_broad/vocab alias both-forms/cluster ops/501+400 contracts). | Accepted |
| [071](decisions/adr-071-grpc-tls-auth.md) | TLS + mesh auth on the gRPC transports | ADR-065 criterion 2: both gRPC surfaces (shard `ShardService` + control-plane `ControlService`) gain two independent, opt-in knobs — **TLS** (tonic `tls-ring`/rustls; operator PEM identity server-side, CA + optional domain override client-side; mTLS deferred — node admission is the token's job at v1) and a **mesh token** (ONE shared cluster secret, `--cluster-token`/`RR_CLUSTER_TOKEN`, ADR-062 validation rules) attached to every RPC as `authorization: Bearer` metadata and verified constant-time BEFORE any handler (the interceptor wraps the whole service ⇒ default-deny covers future RPCs). One `cluster::security` module serves both transports and both sides (client inject / server verify), so the planes can't drift; `RemoteShard` + the Raft network client store interceptor-wrapped channels (zero RPC-call-site churn); additive `_with_security` constructors keep every unset path byte-identical; the coordinator RETAINS its `ClientSecurity` so internal connects (peer recovery, live handoff) ride the same TLS + token. Config is fail-loud (malformed PEM/token refuses startup; token-without-TLS warns loud). Trust model documented: token = mesh admission, TLS = server auth + wire privacy; the HTTP token (ADR-062) stays separate (different audiences/rotation). deny policy: ISC allowed (the rustls/ring chain) and the gate's `cargo deny` widened to `--all-features` so the distributed supply chain is policy-checked. Proven: secured K=2 cluster ≡ brute incl. dict shipping + live writes over TLS+token; wrong/missing token + plaintext-client-to-TLS-server all fail LOUD (never an empty result); a secured 3-node control plane elects + quorum-commits; security-module units (validation/inject/verify round trip). In-test certs via `rcgen` (dev-dep — no key material in the repo). | Accepted |
| [072](decisions/adr-072-multi-machine-harness.md) | The multi-machine test harness — containers, kill-and-recover, handoff under load | ADR-065 criterion 3 (the last unblocking item): a compose-based harness (`deploy/`) exercising the **fully secured** stack (ADR-071 TLS + token) through the **public surfaces only** (the ADR-070 coordinator REST + node bins) across real container network boundaries — the failure class localhost oracles structurally cannot reach (process lifecycle, reconnects, startup ordering, container filesystems). One multi-stage `Dockerfile` (builder + slim runtime; the same image criterion 10 ships) + a prebuilt-bin variant for CI; `compose.harness.yml` = 3 durable shard nodes + a pending handoff target + the REST coordinator + a durable 3-node control-plane quorum; `harness.sh` asserts: baseline; **kill-and-recover** (a dead shard's probes answer 502 fail-loud — NEVER a silently truncated union — and every still-succeeding probe matches baseline exactly; the restarted node self-restores, ADR-039); **rolling restart** ≡ baseline; **coordinator restart** (stateless re-mint + fingerprint handshake against populated shards + load-file skip) ≡ baseline; **live handoff under load** via the new `POST /_cluster/handoff` (the operator surface ADR-044/048 was missing — `execute_handoff` on the blocking pool, fail-closed + auto-unfence on abort) — a write loop runs across the move, every acknowledged write stays matchable (zero FN over real infrastructure), probes ≡ baseline; control-plane rolling restart resumes from durable Raft state. CI runs it on every PR (`multi-machine harness` job: native cached build → prebuilt image → harness). Deferred: multi-host topologies (same images, criterion 10's runbook), partition/latency/disk fault injection, control-plane→coordinator wiring (the ADR-048 deployment-maturity note). | Accepted |
| [074](decisions/adr-074-tagged-cluster-vocab-change.md) | Tagged-cluster vocabulary change — TagId carry-through | ADR-065 criterion 4 (the ADR-055 deferral): `set_vocab`/`learn_and_apply` now work on a **tagged** cluster instead of refusing. Mechanism = **carry stored `TagId`s through the blue/green rebuild verbatim** (the single-node ADR-049 precedent), NOT the hypothesized raw-string persistence: the tag space is orthogonal to vocabulary and preserved (the rebuild re-mints the *feature* dict, never the `TagDict`), interned ids keep their dense slots and synthetic ids are deterministic hashes — so id carry-through is byte-identical reconstruction with zero new durable state, no manifest bump, no synthetic-string side table, and ADR-049's "tag strings die at the boundary" intact. `Engine::live_sources_tagged` + a `Shard::live_sources_tagged` seam gather `(logical, dsl, tag_ids)` per shard; `PlacedQuery.tag_ids` carries them through re-placement (an alias can move a query's shard — its tags travel); the wire stays dict-agnostic (`RemoteShard` refuses non-empty `tag_ids` loudly). The `tags_present` latch demotes to `/_stats` introspection — it was also lossy (a checkpointed synthetic-only cluster restores it `false`, so the old guard would have silently dropped tags after reopen; closed structurally). **Two pre-existing sources-durability bugs found by the new oracle, both fixed:** cluster bulk ingest never wrote `sources.dat` (a checkpointed+reopened cluster gathered an EMPTY corpus — `set_vocab` rebuilt to zero queries), and a clean-shard checkpoint skipped the sources rewrite (a deleted query's source went stale on disk — `set_vocab` after reopen would RESURRECT it). Fixed at both commit points (bulk seal + checkpoint seal, fail-closed per ADR-051). Oracle-proven: K-swept tagged rebuild ≡ brute-with-tags (interned + synthetic, filtered ⊆ unfiltered, re-placed query keeps tags), checkpoint → reopen → rebuild → reopen, the latch-blind-spot + both sources regressions, the wire guard. Non-local + multi-word refusals (ADR-061) stand. | Accepted |
| [075](decisions/adr-075-cluster-ranking.md) | Cluster ranking — rank-at-shard, merge at the coordinator | ADR-065 criterion 5 (the ADR-059 deferral): the `rank` block works in cluster mode. **Rank-at-shard + compile-once-fan** (the ADR-055 pattern; ES query-then-fetch shape): the coordinator compiles the spec ONCE against the ONE shared frozen `TagDict` (`compile_rank_spec`, same `get_or_synthetic` as the filter), a new required `Shard::percolate_filtered_ranked` fans the same `&CompiledRankSpec` to every probed shard, each shard scores its OWN matched ids against its tag columns under ONE snapshot (per-shard newest-live-copy, the single-node semantics), and the coordinator merges + dedups by id — safe because copies of a logical are version-identical across shards (identical op streams), so every shard reports the same score. Wire: `PercolateRequest.rank` (resolved `TagId` boosts + priority key) + `PercolateReply.scores` (parallel) + a **`ranked` echo** the client checks — an old server that ignores the field fails LOUD, never a silently-unranked ordering. Handlers share the single-node `RankBody`, order `(score desc, _id asc)`, slice `from`/`size`, emit `_score`; per-slot and merged-union; `explain` stays the one 400. **Synthetic-tag boundary pinned:** a post-freeze boost fires (id-equality) but a synthetic `priority` tag contributes 0 (priority needs the value STRING only an interned tag has) — inherent to strings-die-at-the-boundary, presentation-only, documented + tested. Ranking stays post-match: ranked-id-set ≡ unranked-id-set at every K (zero FN); no `rank` ⇒ byte-identical. Oracle-proven K-swept ≡ single-node, over real gRPC, and from a reopened cluster. | Accepted |
| [076](decisions/adr-076-cluster-multiword-aliases-vocab-shipping.md) | Multi-word aliases on a cluster (P(T)-aware routing) + the vocab-shipping decision | ADR-065 criterion 6 (the ADR-046/061 deferrals), one half shipped + one half decided-refused. **Shipped: P(T)-aware routing** — `route` derives targets from the maximal positive view when multi-word aliases are active; cover argument: a query's anchor is one of its positive features and P(T) ⊇ every parse's features (the ADR-061 parse-union property), so a satisfying title always probes the anchor's shard (zero FN); P(T)==N(T) when inactive ⇒ byte-identical. All three ADR-061 cluster refusals retired. **Found+fixed the bare-normalizer activation gap:** equivalence-driven vocabulary (declared equivalences + registry aliases, single-token included) was silently INERT on `build(norm)` and on the coordinator-mode `--vocab` startup (which dropped the loaded Vocab) — new **`ClusterEngine::build_with_vocab`** installs the equivalence machinery before extraction (the `Engine::with_vocab` order), installs + persists the vocab in the manifest from the FIRST durable commit; the server's fresh in-process build uses it. **Refused, documented: live cross-process vocab shipping** — remote-cluster vocabulary is deploy-time configuration (the ES analyzer-reindex precedent; a live remote rebuild is handoff-grade machinery and collides with the ADR-074 tag-wire boundary until criterion 9); `set_vocab` keeps the non-local refusal and the server now FAILS STARTUP on a remote assembly given ANY vocab file (equivalences would be inert AND normalizer-level rules would diverge from the shards' stock normalizer — silent cross-form FNs either way); an empty bare-manifest reopen activates the file vocab via the `set_vocab` funnel (a populated one warns). Oracle-proven: K-swept set_vocab activation ≡ single-node, build_with_vocab activation, durable reopen incl. empty-bare-manifest activation, the bare-norm parity boundary. | Accepted |
| [077](decisions/adr-077-tagdict-recovery-fingerprint.md) | Tag-dict fingerprint in the recovery handshakes | ADR-065 criterion 9 (the deferred ADR-055 hardening): the six fingerprint-guarded RPCs (`FetchSegments`/`RecoverFrom`/`FetchTranslog`/`RetentionLease`/`Fence`/`Unfence`) verified only the FEATURE-dict fingerprint, and the bare-`connect` probe could not attest the tag space at all — a node whose tag dict diverged after adoption could serve/accept recovery traffic whose tag columns silently mis-filter. Mirror the dict pattern exactly: `tag_dict_fingerprint` on all six requests (server-side `failed_precondition` on mismatch, the same two-line guard beside the dict check), the probe reply attests BOTH spaces (a bare `connect` verifies + stores `tag_dict_fp`, presented on every guarded RPC; `RecoverFrom`'s outbound `FetchSegments` dial presents its own verified fingerprint). **Version skew loud:** a pre-077 server's probe leaves the field 0 — never equal to a real fingerprint, so the connect refuses (the ADR-075 echo principle, structurally free); the request-field asymmetry (a stale server ignoring the field) is the pre-077 status quo for that hop, gated anyway by the connect/adopt handshakes. Out of scope, documented: the self-restart sidecar (guarded by ADR-072 `tagdict.bin`), the ADR-074 tag-wire boundary (this is its named prerequisite, not a license). Fail-closed only; untagged meshes' finalized-empty fingerprints match by construction ⇒ unchanged. Guard oracle mutation-validated; all 19 gRPC oracles flow the fingerprint end-to-end. | Accepted |
| [078](decisions/adr-078-cluster-resize.md) | Cluster resize — the auto-split mechanism | ADR-065 criterion 7 (*"the ring's `num_shards` stops being fixed at construction"*). A resize is the **`set_vocab` blue/green rebuild with the RING swapped (K→K′) instead of the normalizer**: the shared `rebuild_from_live` core re-places every live query under a fresh `HashRing::new(K′, vnodes)` — placement and routing both read the new ring (the fresh-`build` invariant), so re-placing the whole corpus is internally consistent and the hard "online ring re-keying" problem is sidestepped. Surface: `resize(K′)`, the pure `recommended_shard_count` (= K + #over-threshold shards), `resize_to_recommended`, and `POST /_cluster/resize` (write-locked like `PUT /_vocab`); the autoscaler keeps `RecommendSplit` ADVISORY (always-on auto-execution deferred — a resize is non-idempotent + `O(corpus)`, so it needs hysteresis). **In-process only** (refuses non-local/handoff, the `set_vocab` boundary; cross-process resize is the follow-on). **Durable for free** (no manifest format bump: `checkpoint` writes `num_shards`, `open` re-derives the ring + re-seeds the control plane). The one real delta vs `set_vocab` — the shard-DIRECTORY set changes — is handled: grow force-cleans new dirs + builds fresh via `new_durable` (so a stale dir from a prior shrink can't self-restart into an old corpus; divergent corpus fails loud `DictMismatch`), shrink rebuilds survivors in place + removes orphan dirs AFTER the commit (exactly `shard_000..shard_{K′-1}` on disk), the control plane is re-seeded (`ClusterStateChange::SetShardCount`), `pending_repair` is cleared, and the normalizer/vocab/tags + the dict fingerprint are preserved (invariant). Oracle-proven: resize ≡ single-node ≡ brute across grow/shrink/shrink-to-1/round-trip, broad on/off, tagged, durable reopen, and a mutation-validated delete-then-regrow no-resurrection test. | Accepted |
| [079](decisions/adr-079-backup-restore.md) | Backup/restore — the engine-driven consistent snapshot | ADR-065 criterion 11. A *live* `cp -r` of a `data_dir` is unsafe (ADR-064 item 7): a concurrent compaction commits a new manifest then deletes the superseded `.seg`, so an external copier can capture a manifest referencing a file it missed. **Key insight:** that race exists only because the copier is external — the engine is single-writer (ADR-017, no background LSM thread), so the **engine itself** doing a manifest-driven copy under its own write-path exclusion is race-free by construction, and "restore" is just the already-oracle-proven `open()`. Surface: `Engine::backup_to(&mut self, dest)` (copies manifest-referenced segments + `sources.dat` + `wal.log`; restore replays the WAL tail), `ClusterEngine::backup_to(&self, dest)` (`checkpoint()` FIRST — the ADR-074 seam makes every shard's `sources.dat` durable — then copies the coordinator manifest + `cluster.log` + per-shard segments + `sources.dat`; replica dirs are rebuilt on open, not copied), `verify_backup`/`verify_cluster_backup` (re-open every segment via CRC-checking `MmapSegment::open`), and `POST /_backup {"dest":...}` (both modes, behind the ADR-062 default-deny gate). Atomic via sibling-staging + `durable_rename`, manifest copied LAST, pre-existing `dest` refused. Manifests are COPIED not regenerated ⇒ the v4 class-D fence + embedded dict/vocab/tag-dict survive verbatim; orphan `.seg` skipped. Trade-off recorded: the built-in path is a write-exclusion snapshot (pauses writes, not reads, for the copy); the runbook documents FS-snapshot-of-a-checkpoint'd-dir for zero-stall, and online retention-lease backup is the follow-on. Oracle-proven: single-node + cluster (K∈{1,3,8}×broad, tagged, isolation, idempotent root, fail-loud). | Accepted |
| [080](decisions/adr-080-cluster-replicate-broad-to-all.md) | Replicate-broad-to-all + the cluster class-D lane | ADR-065 criterion 8. Graduates ADR-027's shard-0 broad stand-in: the broad lane (class C + B-arity-2 + opt-in class D) is **replicated to every shard** and evaluated on **one broad-eval shard per title** (free-rides an already-probed selective target, else a `fnv1a64(title)`-hashed shard), so no shard is a broad hotspot and the always-probe-shard-0 invariant is gone. `placement_of` gains `accept_class_d`: class D → the broad lane (under `universal_sig`) when on, else `Reject` — unblocking the ADR-068 always-candidate lane cluster-wide. Write fan-out (`bucket_and_ingest`/build/`rebuild_from_live`/`apply_add`/`apply_upsert`) sends a `Replicated` query to every shard via a shared `insert_on_shards` ADR-047 partial-apply path; `apply_remove`/`replay_apply` unchanged. The v5 **`ClusterManifest`** is the replicate-to-all layout marker (the cluster has no per-shard manifest, ADR-032): every ADR-080 cluster writes v5 — a TWO-WAY fence (a pre-ADR-080 binary fails `open` on v5; this binary refuses a v<5 cluster whose broad is shard-0-only — the silent-FN upgrade path, a codex catch). Lifecycle guards (codex): an effectively-empty class-D rejects before fan-out so a failed upsert never deletes the prior version; a rebuild (resize/set_vocab) forces accept-class-D on so a knob-off reopen keeps its sealed always-candidates; the clog re-derives placement deterministically (no op markers; sealed class D survives a knob flip). **Rejected R-coord** (coordinator-local broad): its only win (no broad-eval hop) is unrealizable pre-multi-coordinator and it would stand up a second durable/replicated matcher off the shard HA stack — deferred. In-process first (wire-transparent); remote shard servers share the `--accept-class-d` operator contract. Oracle-proven: cluster ≡ single-node ≡ brute across K∈{1,3,8,16}×broad, storage-fanout-N vs read-fanout-bounded (hotspot gone), durable reopen + v5 fence + knob-off visibility. | Accepted |
| [082](decisions/adr-082-packaging-deploy-correctness.md) | Packaging follow-on — control-plane advertise-URL + coordinator-gated class-D | Closes two ADR-081 deploy-correctness deferrals (binary + deploy-doc only, no library change). **`controlserver --advertise-url`**: the bootstrap node committed `https://{bind}` into Raft membership, so a containerized `0.0.0.0` bind became an undialable membership entry; the new flag supplies the routable self-URL (factored into a pure, unit-tested `bootstrap_self_url`), and a `--bootstrap` on a wildcard bind WITHOUT it now **fails loud** (a scheme/TLS mismatch warns loud); `compose.cluster.yml`'s `control0` passes `--advertise-url https://control0:50061`. **Coordinator-gated class-D**: the "no `shardserver --accept-class-d`, so class-D is dropped on the remote topology" item (in ADR-081, the coordinator's startup `warn!`, and runbook §11) was a phantom — verifying the code, every remote shard is a `LocalShard`, which FORCES `accept_class_d = true` on every build path (the coordinator is the sole gate, ADR-068/080), so class-D already works over the wire. Removed the misleading warning + §11 bullet, documented the real contract (runbook §3), and locked it with a new `cluster_grpc_oracle::class_d` test (default-config K=3 servers store + serve a class-D query over gRPC: matches a clean title, excludes a forbidding one, quarantined with broad off). Unblocks — does not complete — the deferred `--control-endpoint` wiring. | Accepted |
| [081](decisions/adr-081-deployment-packaging-runbook.md) | Deployment packaging + operations runbook | ADR-065 criterion 10. Ships the production operator packaging over the ADR-072 test image — **no code, deploy files + docs only**: `deploy/compose.cluster.yml` (a K=3/RF=1 production topology distinct from the test `compose.harness.yml` — durable volumes, `unless-stopped`, loopback-published REST, mesh TLS+token, fail-loud required secrets, explicit shard services + inline add-a-shard/RF=2 templates), `cluster.env.example`, `gen-mesh-certs.sh` (the mesh identity — `CA:FALSE` + per-service SANs + refuse-to-clobber-a-live-mesh), `cluster-smoke.sh`, and `docs/operations/cluster-deployment.md` (bootstrap order, health, scale, recover, backup, the ADR-076 vocab redeploy, monitoring, security checklist). Records four deferrals as decisions: **(a)** control-plane↔coordinator wiring — a remote coordinator runs an in-memory control plane and re-derives placement from dict+ring; the durable `controlserver` quorum is shipped but not yet consulted (no `--control-endpoint`), so the runbook flags it loudly; **(b)** k8s manifests (shape sketched, gated on (a)); **(c)** remote add-a-shard = redeploy (ADR-078 resize is in-process only); **(d)** vocab redeploy = deploy-level blue/green (ADR-076). Validated by `docker compose config` (loopback `host_ip` + stateless coordinator + fail-loud secret guard) + `bash -n`; full bring-up is `cluster-smoke.sh` / the ADR-072 harness. | Accepted |
| [083](decisions/adr-083-control-plane-coordinator-wiring.md) | Control-plane ↔ coordinator wiring — the `RemoteControlPlane` client | The ADR-081 deferral (a) follow-on (unblocked by ADR-082's advertise-URL). A deployed coordinator ran the in-memory control plane while the durable openraft quorum sat **idle**, AND the `ControlService` had only the raw Raft peer RPCs — no client surface. Wire the coordinator as a **thin, stateless gRPC client** (it does NOT join consensus — a learner would make the stateless router a Raft member): a new opaque **`ClientControl`** RPC on `ControlService` (serde `control_wire` enums, same byte-pipe as the Raft RPCs); `ControlServer::with_client_plane` serves it as **native async** against the local `RaftControlPlane` (the sync `ControlPlane` methods `block_on`, which would nest on a gRPC worker), reusing the same openraft calls + error mapping (remote ≡ embedded); `ControlServer::new(raft)` stays Raft-only (ADR-038 oracle unchanged; a Raft-only server answers `ClientControl` `unimplemented`). **`RemoteControlPlane`** implements the sync `ControlPlane` trait over the wire (mirrors `RemoteShard`'s `block_on_in_context` bridge), following one **`ForwardToLeader`** redirect (redial leader, retry once); any RPC failure → `ControlError::Backend`, never a swallowed stale map. `ClusterEngine::with_control_plane` swaps the backend; the server's **`--control-endpoint`** (remote-mode only, fail-loud) builds + injects it before ingest; absent ⇒ in-memory backend, byte-identical. **Safety:** the control plane is read only on the admin/introspection path, NEVER during percolation ⇒ zero-FN risk (oracle: `percolate` byte-identical across a `register_node` that commits through the quorum). **Scope:** makes the cluster-state DOCUMENT durable + HA; does NOT route by committed assignments (still the `--shard-endpoint` list) — a separate feature. Oracle-proven (`cluster_control_wiring_oracle`): trait round-trip, coordinator injection + percolate-unchanged, follower→leader forward. | Accepted |
| [084](decisions/adr-084-kubernetes-helm-health.md) | Kubernetes/Helm packaging + gRPC health endpoints | ADR-065 criterion-10 follow-on; ships the k8s manifests ADR-081 deferred (deferral b), unblocked by ADR-083's control-plane wiring. **gRPC health (engine, `distributed`-gated):** the standard `grpc.health.v1.Health` service on an opt-in SEPARATE plaintext `--health-addr` port (default unset ⇒ no second listener, byte-identical) for k8s probes — liveness (`Check("")`) up once serving, readiness (`Check("ready")`) tracks dict-adoption (shard) / leader-known (control) via a 250 ms poll watcher off the hot path. Hand-rolled proto (zero new deps, ADR-028) via the existing protox codegen; only `Check` served (`Watch` → `unimplemented`); separate PLAINTEXT port because the k8s built-in `grpc` probe is plaintext-only and the mesh port is TLS+token-gated. Readiness needs no RPC-handler change — `ShardServer.state` became `Arc<ArcSwapOption<…>>` so the watcher shares the cell `AdoptDict` writes; control clones the `Raft` handle. **Helm chart (`deploy/helm/reverse-rusty/`, no engine code):** the k8s analogue of `compose.cluster.yml` — shard + control `StatefulSet`s (per-pod PVC, native gRPC probes), headless Services with `publishNotReadyAddresses` (dial a pending shard to AdoptDict it), the ordinal-0 control bootstrap dance (`$HOSTNAME`→ordinal, `--advertise-url` per ADR-082), a coordinator `Deployment` (`--shard-endpoint` per shard + `--control-endpoint` to the quorum + a `wait-for-mesh` initContainer), Secret-referenced mesh TLS + tokens, optional Ingress. **Deferred:** routing by committed assignments (the ADR-083 residue), shard/control Prometheus metrics (health ≠ metrics), gRPC `SIGTERM` graceful drain, RF>1/online-resize/remote-vocab (unchanged). **Safety:** off every match hot path + opt-in ⇒ zero-FN (the full `distributed` oracle stays green across the `Arc` refactor). Validated: new health transition tests over real gRPC + `helm lint`/`template`/`kubeconform -strict` (k8s 1.29) across a value matrix, wired as a CI job. | Accepted |
| [085](decisions/adr-085-grpc-transport-hardening.md) | gRPC mesh-transport hardening — timeouts, keepalive, bounded read-retry, RPC metrics | Hardens the experimental distributed transport's biggest correctness gap: the gRPC CLIENT set no connect timeout, no per-RPC deadline, and no keepalive, so a remote shard that ACCEPTS a connection then hangs blocked the coordinator's percolate fan-out (`collect::<Result>?`) **forever**. Four layers, all default-on + byte-identical on the in-process path: **(1) endpoint** — `configure_endpoint` (the SHARED dial helper) now sets `connect_timeout` + HTTP/2 keepalive, so the shard client, control-plane client, AND Raft peer network harden in one place; `MeshTransport` knobs ride `ClientSecurity`, `ServerSecurity` gains server-side keepalive (`ShardServer`/`ControlServer` reclaim dead clients). **(2) one `call` seam** in `RemoteShard` — every RPC funnels through it: per-call deadline via `tokio::time::timeout` (NOT `Endpoint::timeout`, channel-wide → would kill the long streaming recovery RPCs; NOT `Request::set_timeout`, absent in tonic 0.14), `mk` is a FACTORY (single-use call futures rebuilt per attempt), **bounded fail-loud retry of IDEMPOTENT reads** on transient `Unavailable`/timeout with exp backoff, **writes never retry** (non-idempotent → converge via the durable log + `resync`, ADR-047), streaming/`recover_from` opt out of the deadline (keepalive-guarded). A timeout/exhausted-retry → `ShardError` → propagates fail-closed (zero FN; partial-results was REJECTED as anti-contract). **(3) lean metrics** — std-only `transport_metrics.rs` (per-method calls/errors/timeouts/retries/latency) shared into serving shards; `ClusterEngine::transport_metrics()` snapshot → cluster-mode `/_metrics` Prometheus gauges. **(4) coordinator CLI** — `--grpc-*-timeout-secs`/`--grpc-keepalive-secs`/`--grpc-read-retries` (optional, sane defaults). Proven: `remote.rs` unit tests (timeout fires, transient retry recovers, non-transient/write no-retry, backoff bounds) + a real-gRPC oracle (`cluster_grpc_oracle::transport`: happy-path metrics + downed-shard fail-loud-with-retry); full distributed oracle green. Deferred: tracing/correlation IDs, control-plane RPC metrics, a deadline on long recovery RPCs, mTLS/cert-rotation (ADR-071 post-v1). | Accepted |
| [086](decisions/adr-086-control-plane-routing-and-failover.md) | Coordinator routes by committed assignments + multi-control-endpoint failover | Closes the ADR-083 routing residue (roadmap Tier 3). **The trap:** naively resolving `shards[i]` from `assignments[i]` is unsafe today — HRW `rebalance` permutes the committed map WITHOUT moving data (*"the local shards do not move"*), so a post-rebalance restart would point a position at a server holding a different shard (a silent false negative). **Decision (the safe half):** (1) **multi-endpoint failover** — `RemoteControlPlane::connect_failover` tries the whole `--control-endpoint` list (the coordinator stops using `.first()`); `call` fails over to the remaining endpoints on a transport error, ORTHOGONAL to the existing `ForwardToLeader` follow (failover finds a reachable node, forward finds the leader); all-down ⇒ fail loud. (2) **topology resolution** — lean pure-`ControlPlane` `seed_position_preserving` (idempotent; one Data node per distinct URL; overwrites the genesis addr-less `NodeId(0)` map — closing the bootstrap gap) + `resolve_topology` (`position → assignment → node.addr`, fail-closed on unassigned/addr-less), both unit-tested against `InMemoryControlPlane`. (3) **opt-in `--route-by-assignments`** — the coordinator attaches the control plane FIRST, seeds position-preservingly from `--shard-endpoint`, resolves the topology from the committed document (so the quorum, not per-coordinator flags, is the topology SoT — a coordinator can boot with only `--control-endpoint`), and **guards**: a resolved topology that differs from `--shard-endpoint` is a non-position-preserving map ⇒ fail loud. Default + non-opt-in paths byte-identical; hot path untouched (routing still indexes `shards[s]` by ring position) ⇒ zero-FN. **Deferred:** a committed assignment change DRIVING `execute_handoff` so reassignment moves data and routing follows live (the scaffolding exists; needs a zero-FN proof under concurrent writes). Proven: lean seed/resolve/idempotent/guard tests + a distributed redirect oracle (`cluster_grpc_oracle::routing`: reassign A→B redirects a fresh coordinator, sentinel-verified) + a distributed failover oracle (`cluster_control_wiring_oracle`). | Accepted |
| [087](decisions/adr-087-independent-correctness-oracle.md) | A front-end-independent correctness oracle (Phase 0 item 2) | Phase 0 reality-audit item 2 (top priority, above every tier). The in-tree differential oracle's reference calls the engine's OWN `dsl::parse`/`compile::extract`/`Normalizer`, so a front-end bug corrupts both sides identically and it stays green (the ADR-050 shared-front-end blind spot; golden tests are point cases, not a differential). **Decision:** a separate **std-only, zero-dependency** workspace member `reverse-rusty-ref-matcher` reimplements the WHOLE front end from the SPEC (parser; the two-phase normalizer incl. graders + the `>3`/`>2` aging, number/year typing, markers, ADR-061 two views `N(T)`/`P(T)` with the force-additive parse-union + overlap scan; the extractor incl. joint bare-word normalization, the rarest-by-freq any-of proxy + singleton-collapse, ADR-054 equivalence expansion, class-D drop; the predicate) — reusing NONE of the engine (no `reverse-rusty`, no `daachorse`, no `serde`), comparing by canonical feature STRING (no dict) with NAIVE phrase scans (a structurally different impl catches integration bugs a shared automaton hides). Independence is **mechanically enforced** by a `check.sh` `cargo tree` lane. A new `tests/independent_oracle/` harness builds engine + reference from ONE vocab description and asserts zero FN AND zero FP over: generated default (clean + messy), populated graders/phrases, the multi-word alias two-view (controlled + at-scale), a hand-written **gotcha** table (asserted against both sides, the human expectation is the tiebreaker), and an **env-gated real corpus** (`RR_ORACLE_CORPUS`, skipped when unset so CI/the repo never see real data). **Drift policy:** the spec + golden tests are the authority. Deliberately revisits ADR-050's declined "independent extractor" — its three objections (unverified copy / lockstep / un-attributable divergence) are addressed (cross-checked not unverified; bounded maintenance; spec-as-authority). **Findings:** `default_vocab()` has empty graders (so `psa10` does NOT fuse there); one documented simplification (lex- vs id-tiebreak for a multi-token any-of proxy, unobservable on real surface forms). **Result:** zero FN/FP across ~61k default + ~69k messy + ~75k populated + ~989k alias matches + every gotcha — NO engine front-end bug found (the front end is now independently confirmed, not assumed). Lays the ground truth for Phase 0 item 3 (crash injection). Purely additive; lean/server/distributed builds byte-identical (dev-only member). | Accepted |

---

*Conventions for editing docs (and where each fact lives) → [`README.md`](README.md).*
